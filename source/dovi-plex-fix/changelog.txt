0.3.0
- Add NVENC preset setting (p1–p7) for speed/quality tradeoff
- Add CPU fallback option (libx265) when NVENC unavailable, default off
- Add README with installation, requirements, settings, troubleshooting

0.2.1
- Add FFmpeg progress parser so worker percentage updates in web UI
- Queue HEVC video files for processing (add_file_to_pending_tasks)
- Fix DOVI detection: check codec_tag_string at stream root (ffprobe output)
- Add **kwargs to file test runner to avoid conflict with Unmanic's executor
- Fix NVENC check: remove incorrect .D filter (D = direct rendering, not disabled)
- Use dovi_rpu=strip=1 instead of non-existent hevc_metadata=delete_dovi
- Exclude attached pictures from map to avoid MP4 muxing errors (-map 0:v:0 -map 0:a -map 0:s)
- Use NVDEC (hevc_cuvid) for GPU decoding when available to avoid CPU pegging
- Fix GPU pipeline: omit hwaccel_output_format cuda to avoid format conversion error
- Skip dovi_rpu when using hevc_cuvid (cuvid handles DV; dovi_rpu corrupts NAL units)

0.2.0
- Initial Unmanic plugin package
- JSON-based Dolby Vision Profile 5 detection
- NVENC transcode for DV, remux for non-DV
- FFmpeg hevc_metadata=delete_dovi compatibility check
- Skip DV files when NVENC unavailable (no CPU fallback)
- Preserve audio and subtitle tracks
- Configurable settings: Use NVENC, Enable DV metadata removal
